const FilterTag = () => {
  const filterTagMarkup = `
    <span class="filter__tag">Coco 
      <i class="bx bx-x-circle filter__tag-close"></i>
    </span>
  `;

  return filterTagMarkup;
};

export default FilterTag;
